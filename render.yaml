services:
  - type: web
    name: benjamin-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python stable_bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
